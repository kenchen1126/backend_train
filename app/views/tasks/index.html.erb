<h1>任務列表</h1>
<%= link_to "新增任務", new_task_path %>
<% @tasks.each do |task| %>
  <div>
    <%= link_to task.name, task_path(task.id) %>
    <%= link_to "修改", edit_task_path(task.id) %>
    <%= link_to "刪除", task_path(task.id), method: "delete" %>
  </div>
<% end %>